<h1>Профиль</h1>
<div class = "row">
    <div class = "col-md-4">
            <h3><%= current_user.username  %></h3>
    </div>
    <div class = "col-md-8">
        <% if @enrolls.any? %>
            <% @enrolls.each do |enroll| %>
                <h3><%= enroll.course.title %></h3>
                <p><%= enroll.course.description.truncate(37, separator: ' ') %></p>
                <%= link_to 'Continue', course_chapters_path(enroll.course), class: "btn btn-default" %>
          <% end %>
        <% else %>
            <% @allcourses.each do |course| %>
                <h3><%= course.title %></h3>
                <p><%= course.description.truncate(37, separator: ' ') %></p>
                <%= link_to 'Start', enrolls_path(course_id: course.id), method: :post %>
            <% end %>
        <% end %>
    </div>
</div>
<p>общая статистика(сколько пройдено, осталось сколько и т.д.)</p>